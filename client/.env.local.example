# Local Development Environment Variables
# This file is used for local development (npm run dev)
# Copy this to .env.local if you want to override defaults

# API Base URL - Full API endpoint URL (defaults to http://localhost:8000/api if not set)
# VITE_API_URL=http://localhost:8000/api

# API Base Origin - Base URL without /api (defaults to http://localhost:8000 if not set)
# VITE_API_BASE=http://localhost:8000

# API Base URL (alternative name used in some services)
# VITE_API_BASE_URL=http://localhost:8000
